create a full backend with Node.js and Express with flutter with a complete youtubeClone
1. Then Setup the github as we wish

2. Then create a important file called .env, touch with constant.js, index.js, app.js

3. then create folder structure under src mkdir controllers db middlewares models routes utils 

4. then set the prettierrc 
5. then setup the DATABASE


topic multer for file uploading features


multer without express  
